# Copy this file to .env and fill in your real values. Never commit .env itself.

# Get this from MongoDB Atlas -> Connect -> "Connect your application"
MONGODB_URI=mongodb://bhavya497sharma_db_user:N5j0osNiO4LlSEDE@cluster0-shard-00-00.twnj4rf.mongodb.net:27017,cluster0-shard-00-01.twnj4rf.mongodb.net:27017,cluster0-shard-00-02.twnj4rf.mongodb.net:27017/nexusweave?ssl=true&replicaSet=atlas-cluster-xxxx&authSource=admin&retryWrites=true&w=majority

# Any long random string. Generate one with: openssl rand -hex 32
JWT_SECRET=JWSHP46m89XKxZB0OREurMzyDcAspUjGNe3F2LoVIglfd1ht

# How long login sessions last
JWT_EXPIRES_IN=4h

# Port the API server runs on
PORT=4000

# The URL your frontend is served from (for CORS). Adjust if different.
CLIENT_ORIGIN=http://localhost:5500
